import { RentalDirective } from './rental.directive';

describe('RentalDirective', () => {
  it('should create an instance', () => {
    const directive = new RentalDirective();
    expect(directive).toBeTruthy();
  });
});
